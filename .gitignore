__pycache__/
*.pyc
.venv/
dist/
build/
*.zip
inventory/
app/build/
app/.dart_tool/
dist/

# Grafana and Prometheus write into these; the definitions are tracked.
monitoring/grafana/data/
monitoring/data/
tests/.tree/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
coverage.xml

# Both hold an ntfy credential; the example file is the tracked one.
gateway.toml
.env
tests/.tree-msg/

# Локальная инструкция по развёртыванию, не для публикации.
DEPLOY.local.md

# Черновики вырезанных кусков документации, не для публикации.
tmp/
