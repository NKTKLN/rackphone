<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 13 gates both arrival alerts and the foreground service's
         permanent explanation behind notification permission. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- The live gateway connection must remain active when no screen is
         visible, which requires Android's foreground-service contract. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- The service continuously receives gateway event data, so Android 14
         requires the dataSync foreground-service type permission. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <!-- The CPU must stay awake long enough to receive and process SSE frames
         while the display is asleep. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Requested only when accepting a call; no background recording occurs. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Incoming calls may wake the dialer surface over the lock screen. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <application
        android:label="Rackphone"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- The foreground-task plugin owns the Android service while Dart owns
             its one authenticated SSE connection. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
